/* import {Layout} from 'antd';
import './Footer.sass'; */
import './Navbar.sass';

const Navbar = () => {
    return (
        <nav className="navbar">
            <div className="logo"></div>
        </nav>
    );
};

export default Navbar;